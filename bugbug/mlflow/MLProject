name: spambug_training

docker_env:
  image: python:3.9

entry_points:
  main:
    parameters:
      model_to_train: {type: str, default: "spambug"}
    command: "python -m scripts.trainer {model_to_train}"
